<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bruce.designer.dao.FeedLiveIndexMapper" >
    <select id="getIndex" resultType="long">
    	select feed_id from tb_feed_index_live where user_id = #{targetId} limit #{offset}, #{limit}
    </select>
    
    <insert id="insert">
    	insert into tb_feed_index_live (feed_id, user_id) values (#{feedId}, #{targetId})
    </insert>
</mapper>
